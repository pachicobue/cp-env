#include <bits/stdc++.h>
#include <iostream>
#include "internal/abbreviation.hpp"
#include "utility/rng.hpp"

// fhoweho
// ofhaohefoa

int main() {
    int A, B;
    std::cin >> A >> B;
    assert(A > 0);
    assert(B > 0);
    std::cerr << A * B << std::endl;
    std::cout << A + B << std::endl;
    return 0;
}
